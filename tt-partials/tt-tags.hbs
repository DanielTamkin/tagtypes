{{#has tag="!other"}}
  {{! dont show anything.}}
{{else}}
  {{#if seporator}}
    {{#foreach tags}}
      {{! Style the tag loop how you would like}}
       {{name}} {{../seporator}}
    {{/foreach}}
  {{/if}}
{{/has}}
